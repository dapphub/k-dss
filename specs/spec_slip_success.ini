[suck]
k: #execute => #halt
statusCode: _ => EVMC_SUCCESS
output: _ => _
callData: #abiCallData("slip", #bytes32(Ilk), #address(Guy), #int256(Wad))
localMem: _
pc: 0 => _
wordStack: .WordStack => _
gas: Gas => _
log: _
callDepth: _
refund: _
storage:
  // 0 |-> Root
  // 9 |-> #unsigned(V_p)
  // 10 |-> #unsigned(P)
  // 11 |-> (#unsigned(V) => V -Int Delta_D)
  // 12 |-> G
  // (#hashedLocation("DappHub", 4, V_u) |-> (#unsigned(D_u) => D_u +Int Delta_D))
  _:Map
activeaccounts:
accounts:
requires:
  andBool Gas >Int 100000
  andBool #rangeAddress(Guy)
  andBool #rangeSInt(256, Wad)
  
  // success conditions (in order):
  
attribute:

